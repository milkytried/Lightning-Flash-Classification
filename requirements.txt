# Lightning Flash Classification — pinned, resolver-verified dependencies
# Verified to co-resolve on Python 3.10–3.12 (CPU) via `pip install --dry-run`.
# For CUDA builds of torch/torchvision, install those two from the official
# PyTorch index instead: https://pytorch.org/get-started/locally/

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Deep learning (CPU wheels by default) ---
torch==2.12.1+cpu
torchvision==0.27.1+cpu

# --- Numerics / data ---
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2

# --- Satellite / geospatial I/O ---
h5py==3.11.0
xarray==2024.7.0
netCDF4==1.7.2
pyproj==3.6.1
satpy==0.53.0
pyresample==1.31.0
s3fs==2024.10.0
boto3==1.35.36
bottleneck==1.4.2

# --- Imaging / augmentation ---
Pillow==10.4.0
opencv-python==4.10.0.84
albumentations==1.3.1

# --- Training utilities ---
tensorboard==2.18.0
tqdm==4.66.5
schedule==1.2.2
pyyaml==6.0.2

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
